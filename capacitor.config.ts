import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'snacks_ecommerce',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
